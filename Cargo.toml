[package]
name = "rust"
version = "0.2.0"
edition = "2024"

[dependencies]
thiserror = "1.0"
tokio = { version = "1.0", features = ["full"], optional = true }

[features]
default = ["tokio"]
tokio = ["dep:tokio"]

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
